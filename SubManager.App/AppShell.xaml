<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SubManager.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SubManager.App"
    xmlns:views="clr-namespace:SubManager.App.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="SubManager.App">

    <ShellContent 
        ContentTemplate="{DataTemplate views:SubscriptionListPage}" 
        Route="list"/>

    <ShellContent 
        ContentTemplate="{DataTemplate views:SubscriptionEditPage}" 
        Route="edit"/>
</Shell>
